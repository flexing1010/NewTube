extends base

block content  
    form(method="POST")
        input(placeholder="email", type="email", required, value=loggedInUser.name)
        input(placeholder="Username",name="username", type="text",required, value=loggedInUser.email)
        input(placeholder="Name", name="name", type="text",required, value=loggedInUser.name)
        input(placeholder="Location",name="loaction", type="text", value=loggedInUser.location)
        input(type="submit", value="Update Profile")